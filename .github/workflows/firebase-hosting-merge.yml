# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

       # ✅ 여기에 추가
      - name: Generate .env file with clean secrets
        run: |
          clean() { echo "$1" | sed -e 's/^\"//' -e 's/\"$//' -e "s/^'//" -e "s/'$//"; }

          echo "VITE_FIREBASE_API_KEY=$(clean \"${{ secrets.VITE_FIREBASE_API_KEY }}\")" >> .env
          echo "VITE_FIREBASE_AUTH_DOMAIN=$(clean \"${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}\")" >> .env
          echo "VITE_FIREBASE_PROJECT_ID=$(clean \"${{ secrets.VITE_FIREBASE_PROJECT_ID }}\")" >> .env
          echo "VITE_FIREBASE_STORAGE_BUCKET=$(clean \"${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}\")" >> .env
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=$(clean \"${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}\")" >> .env
          echo "VITE_FIREBASE_APP_ID=$(clean \"${{ secrets.VITE_FIREBASE_APP_ID }}\")" >> .env
          echo "VITE_TMDB_API_KEY=$(clean \"${{ secrets.VITE_TMDB_API_KEY }}\")" >> .env
          echo "VITE_SERVER_IP=$(clean \"${{ secrets.VITE_SERVER_IP }}\")" >> .env
          
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MOVIEREVIEW_5ACD9 }}
          channelId: live
          projectId: moviereview-5acd9
